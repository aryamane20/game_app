# Example usage in Claude Code terminal:

# Run security audit on your IAP implementation
"Use the security-auditor agent to review stores/progressStore.ts 
and utils/iap.ts for vulnerabilities"

# Run performance audit on the canvas
"Use the performance-auditor agent to review components/KnotCanvas.tsx"

# Check game feel before shipping
"Use the game-feel-auditor agent to review the snap animation 
in components/SnapAnimation.tsx against the feel standards"

# Full pre-launch check
"Run the ux-guardrail agent across all screens in the app/ directory"